import { Component } from '@angular/core';

// Componente eliminado: stub para evitar errores residuales hasta que se limpie por completo
@Component({ selector: 'app-report', template: '' })
export class ReportComponent {}
